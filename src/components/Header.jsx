import styles from "/src/styles/header.module.scss";

const Header = () => {
  return (
    <header>
      <h1>Onitsuka Tiger</h1>
      <nav>
        <span>shop</span>
        <span>cart</span>
      </nav>
    </header>
  );
};

export default Header;
